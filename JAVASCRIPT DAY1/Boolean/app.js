let sach=true;
console.log(sach);

// 1. General equality(==) -> datatypes are not compared
// comparison (==);
// typecasting -> dharamparivartan
// typecasting of 1 is done it gives true becAUSE it gives false only when number is 0
let ans=sach==1;
console.log(ans);

// 2. Strict equality(===) -> Datatypes are compared but no typecasting is done
let ans1=sach==="1";
console.log(ans1);